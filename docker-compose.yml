services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: image-lista_super:latest
    restart: always
    container_name: container-lista_super
    ports:
      - "13000:80"